# Scenario teaching handouts

This directory preserves reproducible teaching material generated from the
scenario explorer data. Run `create_image_scenario_handout.py` from the
`apps/scenario_explorer` directory to rebuild the accompanying PDF from the
Premise 2.4.8 dataset.

Reports are source material and are excluded from the Heroku runtime slug.
